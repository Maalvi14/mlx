[build-system]
requires = [
  "setuptools>=80",
  "nanobind==2.4.0",
  "cmake>=3.25,<4.1",
]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
cuda = [
  "nvmath-python>=0.2.0",  # NVIDIA cuFFT support for CUDA backend
]
